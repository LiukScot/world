{
  "name": "world",
  "private": true,
  "packageManager": "bun@1.4.2",
  "scripts": {
    "dev": "./scripts/dev.sh",
    "dev:docker": "./scripts/dev-docker.sh",
    "setup": "bun install && bun install --cwd backend && bun install --cwd frontend",
    "dev:frontend": "bun run --cwd frontend dev -- --host 127.0.0.1 --strictPort",
    "dev:backend": "./scripts/run-bun.sh --cwd backend --watch src/server.ts",
    "dev:backend:docker": "docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d --build --remove-orphans world",
    "dev:backend:local": "./scripts/run-bun.sh --cwd backend --watch src/server.ts",
    "dev:stop": "./scripts/stop-frontend-dev.sh && docker compose -f docker-compose.yml -f docker-compose.dev.yml down --remove-orphans",
    "build": "bun run --cwd frontend build",
    "typecheck": "bun run --cwd backend typecheck && bun run --cwd frontend build",
    "migrate": "./scripts/run-bun.sh --cwd backend src/migrate.ts",
    "user": "./scripts/run-bun.sh --cwd backend src/user-cli.ts",
    "backup": "./scripts/run-bun.sh --cwd backend src/backup-db.ts",
    "restore": "./scripts/run-bun.sh --cwd backend src/restore-db.ts",
    "test:unit": "bun run --cwd backend test",
    "smoke:seed": "DB_PATH=${DB_PATH:-./data/smoke-world.sqlite} ./scripts/run-bun.sh --cwd backend src/migrate.ts --fresh && DB_PATH=${DB_PATH:-./data/smoke-world.sqlite} ./scripts/run-bun.sh --cwd backend src/user-cli.ts create --email=smoke@example.com --password=Password123 --name=Smoke || true",
    "smoke:serve": "bun run build && HOST=127.0.0.1 PORT=${PORT:-5555} DB_PATH=${DB_PATH:-./data/smoke-world.sqlite} ./scripts/run-bun.sh --cwd backend src/server.ts",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
